import BaseLayout from "../layouts/BaseLayout.astro";
import SiteFooter from "../components/SiteFooter.astro";
import SiteHeader from "../components/SiteHeader.astro";

const plans = [
  {
    name: "One Class Per Week",
    price: "$72",
    detail: "billed every 4 weeks",
    points: ["$18 per class", "25-minute classes with a native speaker", "Cancel anytime"]
  },
  {
    name: "Two Classes Per Week",
    price: "$120",
    detail: "billed every 4 weeks",
    points: ["$15 per class", "25-minute classes with a native speaker", "Best balance for progress"]
  },
  {
    name: "Three Classes Per Week",
    price: "$180",
    detail: "billed every 4 weeks",
    points: ["$15 per class", "25-minute classes with a native speaker", "Best for faster momentum"]
  }
];

const comparePoints = [
  {
    title: "Best for beginners",
    text: "One class per week works well for families testing consistency or starting from zero."
  },
  {
    title: "Best balance",
    text: "Two classes per week usually gives the strongest mix of routine, retention, and value."
  },
  {
    title: "Best for momentum",
    text: "Three classes per week suits children who enjoy language learning and want faster progress."
  }
];
---

<BaseLayout
  title="Pricing | Fluentz Kidz"
  description="View Fluentz Kidz pricing for weekly live Spanish classes for kids."
  path="/pricing"
>
  <SiteHeader currentPath="/pricing" />
  <main>
    <section class="page-hero">
      <div class="container hero-grid">
        <div>
          <span class="pill">Pricing</span>
          <h1 class="section-title">Simple weekly plans with no long-term lock-in.</h1>
        </div>
        <p class="section-copy">
          Start with 2 free trial classes. After that, choose the weekly plan that fits your
          child’s schedule, attention span, and learning pace.
        </p>
      </div>
    </section>

    <section class="plans">
      <div class="container plans-grid">
        {plans.map((plan, index) => (
          <article class={`card plan-card ${index === 1 ? "featured" : ""}`}>
            {index === 1 && <span class="tag">Most Popular</span>}
            <h2>{plan.name}</h2>
            <div class="price-row">
              <strong>{plan.price}</strong>
              <span>{plan.detail}</span>
            </div>
            <ul>
              {plan.points.map((point) => <li>{point}</li>)}
            </ul>
            <a class={`button ${index === 1 ? "primary" : "secondary"}`} href="/contact">
              Try 2 Classes Free
            </a>
          </article>
        ))}
      </div>
    </section>

    <section class="faq">
      <div class="container faq-grid">
        <article class="card faq-card">
          <h3>What do families do first?</h3>
          <p>
            Start with the first 2 free classes. After that, choose the weekly schedule that fits
            your child’s pace and your family routine.
          </p>
        </article>

        <article class="card faq-card">
          <h3>Do plans require a long contract?</h3>
          <p>
            No. Billing runs every 4 weeks, and families can cancel anytime.
          </p>
        </article>
      </div>
    </section>

    <section class="compare">
      <div class="container compare-grid">
        {comparePoints.map((item) => (
          <article class="card compare-card">
            <h3>{item.title}</h3>
            <p>{item.text}</p>
          </article>
        ))}
      </div>
    </section>
  </main>
  <SiteFooter />

  <style>
    .page-hero,
    .plans,
    .faq,
    .compare {
      padding: 4rem 0;
    }

    .hero-grid,
    .faq-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 2rem;
      align-items: start;
    }

    .plans-grid,
    .compare-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 1.25rem;
    }

    .plan-card,
    .faq-card,
    .compare-card {
      padding: 1.5rem;
    }

    .plan-card {
      position: relative;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .plan-card.featured {
      border-color: rgba(255, 122, 89, 0.22);
      transform: translateY(-0.35rem);
    }

    .tag {
      position: absolute;
      top: 1rem;
      right: 1rem;
      padding: 0.45rem 0.75rem;
      border-radius: 999px;
      background: rgba(255, 122, 89, 0.12);
      color: var(--brand-deep);
      font-size: 0.82rem;
      font-weight: 800;
    }

    .plan-card h2,
    .faq-card h3 {
      margin: 0;
    }

    .price-row strong {
      display: block;
      font-family: "Fredoka", sans-serif;
      font-size: 3rem;
      line-height: 1;
    }

    .price-row span,
    .plan-card ul,
    .faq-card p,
    .compare-card p {
      color: var(--muted);
      line-height: 1.8;
    }

    .compare-card h3 {
      margin-top: 0;
      margin-bottom: 0.75rem;
    }

    .compare-card p {
      margin: 0;
    }

    .plan-card ul {
      margin: 0;
      padding-left: 1.25rem;
      flex: 1;
    }

    @media (max-width: 900px) {
      .hero-grid,
      .faq-grid,
      .plans-grid,
      .compare-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</BaseLayout>
